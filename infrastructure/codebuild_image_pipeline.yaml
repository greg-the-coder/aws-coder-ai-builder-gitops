AWSTemplateFormatVersion: '2010-09-09'
Description: >
  CodeBuild pipeline to build and push the Coder deployment container image
  to a public ECR repository. The resulting image pre-bakes kubectl, eksctl,
  helm, terraform, coder CLI, and AWS CLI so the main deployment build has
  zero download dependencies in its pre_build phase.

Parameters:
  ECRPublicRepositoryName:
    Type: String
    Default: coder-codebuild
    Description: Name of the public ECR repository

  HelmVersion:
    Type: String
    Default: "3.16.4"

  CoderVersion:
    Type: String
    Default: "2.30.2"

  SourceBucket:
    Type: String
    Description: S3 bucket containing the zipped source (Dockerfile + buildspec)

  SourceObjectKey:
    Type: String
    Description: S3 object key for the zipped source
    Default: "codebuild-image-source.zip"

Resources:

  #####################################
  # Public ECR Repository
  #####################################
  PublicECRRepository:
    Type: AWS::ECR::PublicRepository
    Properties:
      RepositoryName: !Ref ECRPublicRepositoryName
      RepositoryCatalogData:
        AboutText: >
          Pre-built container image for Coder EKS deployment CodeBuild jobs.
          Includes kubectl, eksctl, helm, terraform, coder CLI, and AWS CLI.
        OperatingSystems:
          - Linux
        Architectures:
          - x86-64

  #####################################
  # Lambda to discover ECR Public alias
  #####################################
  ECRAliasLookupRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: ECRPublicDescribe
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - ecr-public:DescribeRegistries
                Resource: "*"

  ECRAliasLookupFunction:
    Type: AWS::Lambda::Function
    DependsOn: PublicECRRepository
    Properties:
      Runtime: python3.12
      Handler: index.handler
      Role: !GetAtt ECRAliasLookupRole.Arn
      Timeout: 30
      Code:
        ZipFile: |
          import boto3
          import json
          import urllib.request
          import logging

          logger = logging.getLogger()
          logger.setLevel(logging.INFO)

          def handler(event, context):
              logger.info(f"Event: {json.dumps(event)}")
              response_data = {}

              try:
                  if event["RequestType"] == "Delete":
                      send(event, context, "SUCCESS", {})
                      return

                  client = boto3.client("ecr-public", region_name="us-east-1")
                  result = client.describe_registries()
                  alias = result["registries"][0]["aliases"][0]["name"]
                  logger.info(f"Discovered ECR Public alias: {alias}")
                  send(event, context, "SUCCESS", {"Alias": alias})

              except Exception as e:
                  logger.error(f"Error: {e}")
                  send(event, context, "FAILED", {"Reason": str(e)})

          def send(event, context, status, data):
              body = json.dumps({
                  "Status": status,
                  "Reason": data.get("Reason", "See CloudWatch"),
                  "PhysicalResourceId": context.log_stream_name,
                  "StackId": event["StackId"],
                  "RequestId": event["RequestId"],
                  "LogicalResourceId": event["LogicalResourceId"],
                  "Data": data,
              })
              req = urllib.request.Request(
                  url=event["ResponseURL"],
                  data=body.encode("utf-8"),
                  headers={"content-type": "", "content-length": str(len(body))},
                  method="PUT",
              )
              with urllib.request.urlopen(req) as resp:
                  logger.info(f"CFN response status: {resp.getcode()}")

  ECRPublicAlias:
    Type: Custom::ECRPublicAlias
    DependsOn: PublicECRRepository
    Properties:
      ServiceToken: !GetAtt ECRAliasLookupFunction.Arn

  #####################################
  # IAM Role for Image Build
  #####################################
  ImageBuildRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: coder-image-build-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: ImageBuildPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: CloudWatchLogs
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: "*"
              - Sid: ECRPublicAuth
                Effect: Allow
                Action:
                  - ecr-public:GetAuthorizationToken
                  - sts:GetServiceBearerToken
                Resource: "*"
              - Sid: ECRPublicPush
                Effect: Allow
                Action:
                  - ecr-public:BatchCheckLayerAvailability
                  - ecr-public:InitiateLayerUpload
                  - ecr-public:UploadLayerPart
                  - ecr-public:CompleteLayerUpload
                  - ecr-public:PutImage
                Resource: !Sub "arn:aws:ecr-public::${AWS::AccountId}:repository/${ECRPublicRepositoryName}"
              - Sid: S3Source
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                Resource: !Sub "arn:aws:s3:::${SourceBucket}/${SourceObjectKey}"

  #####################################
  # CodeBuild Project – Image Builder
  #####################################
  ImageBuildProject:
    Type: AWS::CodeBuild::Project
    Properties:
      Name: coder-codebuild-image-builder
      Description: Builds the pre-baked container image for Coder deployment builds
      ServiceRole: !GetAtt ImageBuildRole.Arn
      Artifacts:
        Type: NO_ARTIFACTS
      Environment:
        Type: LINUX_CONTAINER
        ComputeType: BUILD_GENERAL1_MEDIUM
        Image: aws/codebuild/amazonlinux2-x86_64-standard:5.0
        PrivilegedMode: true
        EnvironmentVariables:
          - Name: ECR_PUBLIC_ALIAS
            Value: !GetAtt ECRPublicAlias.Alias
          - Name: IMAGE_REPO_NAME
            Value: !Ref ECRPublicRepositoryName
          - Name: HELM_VERSION
            Value: !Ref HelmVersion
          - Name: CODER_VERSION
            Value: !Ref CoderVersion
      Source:
        Type: S3
        Location: !Sub "${SourceBucket}/${SourceObjectKey}"
        BuildSpec: docker/buildspec_image.yaml
      TimeoutInMinutes: 30
      LogsConfig:
        CloudWatchLogs:
          Status: ENABLED
          GroupName: /aws/codebuild/coder-image-builder
          StreamName: build-log

Outputs:
  ECRPublicRepositoryUri:
    Description: Public ECR repository URI for the Coder CodeBuild image
    Value: !Sub "public.ecr.aws/${ECRPublicAlias.Alias}/${ECRPublicRepositoryName}"

  ECRPublicAliasValue:
    Description: Your auto-discovered ECR Public registry alias
    Value: !GetAtt ECRPublicAlias.Alias

  ImageBuildProjectName:
    Description: CodeBuild project name for building the container image
    Value: !Ref ImageBuildProject

  ECRPublicRepositoryArn:
    Description: ARN of the public ECR repository
    Value: !GetAtt PublicECRRepository.Arn
